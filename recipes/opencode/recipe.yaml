context:
  version: "1.18.32"

package:
  name: opencode
  version: ${{ version }}

# Repack official platform binaries. The npm linux-arm64 package unexpectedly
# requires /lib/ld-musl-aarch64.so.1, so use the upstream glibc release asset.
source:
  - if: target_platform == "osx-arm64"
    then:
      url: https://registry.npmjs.org/opencode-darwin-arm64/-/opencode-darwin-arm64-${{ version }}.tgz
      sha256: a1707bb6cc9deaccca501c4097f1580b8af70dfc227f194ae0f576f32abbdebe
  - if: target_platform == "linux-64"
    then:
      url: https://registry.npmjs.org/opencode-linux-x64/-/opencode-linux-x64-${{ version }}.tgz
      sha256: da0803c85eb86709c4f084adcbfb0b5329936670bfe43d2367f0ca034be0c1de
  - if: target_platform == "linux-aarch64"
    then:
      url: https://github.com/anomalyco/opencode/releases/download/v${{ version }}/opencode-linux-arm64.tar.gz
      sha256: 568461b7d4d8c19865c97e9a1102e613049c6039d01fe772154de873c1865840

build:
  number: 1  # autobump: preserve-build-number
  script: build.sh
  dynamic_linking:
    binary_relocation: false
    missing_dso_allowlist:
      - "**"

requirements:
  run: []

tests:
  - package_contents:
      bin:
        - opencode
  - script:
      - opencode --version

about:
  homepage: https://opencode.ai
  repository: https://github.com/anomalyco/opencode
  license: MIT
  summary: Open source coding agent
